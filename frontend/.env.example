# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================
#
# IMPORTANT: Understanding NEXT_PUBLIC_ Prefix
# ---------------------------------------------
# - NEXT_PUBLIC_* variables are EXPOSED to the browser (client-side code)
# - Use NEXT_PUBLIC_ ONLY for non-sensitive data that needs to be accessible in React components
# - Variables WITHOUT this prefix are SERVER-SIDE ONLY (API routes, middleware, server components)
# - NEVER prefix secrets, API keys, or tokens with NEXT_PUBLIC_
#
# ============================================

# --------------------------------------------
# CLIENT-SIDE VARIABLES (Browser-Accessible)
# --------------------------------------------
# These are bundled into the client JavaScript and visible to users

# Sentry DSN for client-side error tracking
# Used in: Browser error monitoring, component error boundaries
# Required: Yes (for production error tracking)
NEXT_PUBLIC_SENTRY_DSN=

# --------------------------------------------
# SERVER-SIDE ONLY VARIABLES (Secure)
# --------------------------------------------
# These are NEVER exposed to the browser - safe for secrets

# Sentry DSN for server-side error tracking (API routes, server components)
# Used in: Server-side error monitoring, API error tracking
# Required: Yes (for production error tracking)
# NOTE: Can be the same as NEXT_PUBLIC_SENTRY_DSN if using same Sentry project
FRONTEND_SENTRY_DSN=

# GitHub Personal Access Token for MCP server access
# Used in: Development tools, Claude Code integration
# Required: No (development only)
# Scopes needed: repo (if accessing private repos)
GITHUB_TOKEN=

# --------------------------------------------
# FUTURE VARIABLES (To be added)
# --------------------------------------------
# Uncomment and configure as needed:

# Backend API URL
# NEXT_PUBLIC_API_URL=http://localhost:8000
# API_URL=http://localhost:8000

# Paddle Payment Gateway (Production)
# NEXT_PUBLIC_PADDLE_VENDOR_ID=
# NEXT_PUBLIC_PADDLE_ENVIRONMENT=sandbox
# PADDLE_API_KEY=

# OpenAI API Key (server-side only)
# OPENAI_API_KEY=

# Database connection (if needed for direct access)
# DATABASE_URL=

# Redis connection (if needed for session/cache)
# REDIS_URL=
